# Copy to .env and update
PORT=4000

# Example local Postgres connection URL
# Replace user, password, host, port and dbname as needed
DATABASE_URL="postgresql://user:password@localhost:5432/notesdb?schema=public"

NODE_ENV=development

# JWT secret for HS256 (use a long random string in production)
JWT_SECRET=7004fe478ad3186dbb542815e3c03cb2a157faaf85d3863450091c06373471d0

# Access token lifetime (string format accepted by jsonwebtoken, e.g. "15m", "1h")
ACCESS_TOKEN_EXPIRES=15m

# Refresh token lifetime in days (integer)
REFRESH_TOKEN_EXPIRES_DAYS=30

# HttpOnly cookie name for refresh tokens
REFRESH_TOKEN_COOKIE_NAME=refreshToken