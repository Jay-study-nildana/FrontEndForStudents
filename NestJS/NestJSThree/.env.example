# Environment variables declared in this file are NOT automatically loaded by Prisma.
# Please add `import "dotenv/config";` to your `prisma.config.ts` file, or use the Prisma CLI with Bun
# to load environment variables from .env files: https://pris.ly/prisma-config-env-vars.

# Prisma supports the native connection string format for PostgreSQL, MySQL, SQLite, SQL Server, MongoDB and CockroachDB.
# See the documentation for all the connection string options: https://pris.ly/d/connection-strings

# The following `prisma+postgres` URL is similar to the URL produced by running a local Prisma Postgres
# server with the `prisma dev` CLI command, when not choosing any non-default ports or settings. The API key, unlike the
# one found in a remote Prisma Postgres URL, does not contain any sensitive information.

DATABASE_URL="postgresql://postgres:@db.yyhyrmyvetvxirgdhwfc.supabase.co:5432/postgres"

# postgresql://postgres:[YOUR-PASSWORD]@db.yyhyrmyvetvxirgdhwfc.supabase.co:5432/postgres

# JWT settings
# Generate a long random secret for production. Example: openssl rand -hex 64
JWT_SECRET=""

# Access token lifetime (recommended 15 minutes)
JWT_EXPIRES_IN="15m"

# Refresh token lifetime in days (recommended 7 days)
REFRESH_TOKEN_EXPIRES_DAYS="7"

# Password hashing rounds for bcrypt (10-12 is reasonable; 12 is default here)
BCRYPT_SALT_ROUNDS="12"